package com.database;

public class CallableStatements {
	/*
	 JDBC API support In out inout params
	 Stored procedure will also return the resultset
	 
	 CallableStatements statement = connection.prepareCall("{call procedure_name(?,?)}");
	 statement.setString(1,"value");
	 statement.execute();
	 
	 
	 */
}
